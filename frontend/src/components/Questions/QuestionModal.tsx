import { Modal } from '@mui/material';

interface QuestionModalProps {
  open: boolean;
  setOpen: (open: boolean) => void;
}

const QuestionModal = (props: QuestionModalProps): JSX.Element => {};

export default QuestionModal;
